import { DataFlowEllipsisPipe } from './data-flow-ellipsis.pipe';

describe('DataFlowStylePipe', () => {
  it('create an instance', () => {
    const pipe = new DataFlowEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
