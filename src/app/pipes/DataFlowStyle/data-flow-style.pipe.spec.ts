import { DataFlowStylePipe } from './data-flow-style.pipe';

describe('DataFlowStylePipe', () => {
  it('create an instance', () => {
    const pipe = new DataFlowStylePipe();
    expect(pipe).toBeTruthy();
  });
});
